services:
  bnc-backend:
    build: .
    image: bnc-backend:local
    ports:
      - "8000:8000"
    environment:
      NETBOX_URL: ${NETBOX_URL:-https://netbox.example.com}
      NETBOX_TOKEN: ${NETBOX_TOKEN:-}
      NETBOX_TAG_EXTERNAL_CTRL: ${NETBOX_TAG_EXTERNAL_CTRL:-external-ctrl-bnc}
      NETBOX_TAG_STATE_MANAGE: ${NETBOX_TAG_STATE_MANAGE:-bnc-state-manage}
      NETBOX_VERIFY_SSL: ${NETBOX_VERIFY_SSL:-true}
      NETBOX_WEBHOOK_SECRET: ${NETBOX_WEBHOOK_SECRET:-}
      LOG_LEVEL: ${LOG_LEVEL:-INFO}
